class Jumper:
    '''
    
    
    Needs to have a function called 
    
    get_parachute(), that gets the current 
    state of the parachute and returns it. 

    update_parachute(), that will take result (boolean) as a parameter
    if result is False it will delete part of the parachute, 
    if it is True things stay the same. 

    check_parachute(), checks if it is possible to play still,
    return false if not. 
    return true if yes.


    '''