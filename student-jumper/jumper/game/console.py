class Console: 
    '''
    needs to have 
    Write() method that takes a string as a parameter. 

    Read_letter() method gets the input from the player
    checks if it is a single valid letter return error and ask the user again. 
    #this can be done with a while loop and an if. 
    # if letter.lower() in ['a','b',...'z']: 
    #   keep going ... 
    # else:
    #   keep loop ... 
    Return it. 
    
    '''